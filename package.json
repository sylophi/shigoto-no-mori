{
  "private": true,
  "scripts": {
    "postinstall": "pnpm -C app install && pnpm -C app run postinstall",
    "start": "pnpm -C app start",
    "dev": "pnpm -C app dev",
    "dev:peer": "pnpm -C app dev:peer",
    "test": "pnpm -C app test",
    "typecheck": "pnpm -C app typecheck",
    "lint": "pnpm -C app lint",
    "format": "pnpm -C app format",
    "format:check": "pnpm -C app format:check",
    "web:dev": "pnpm -C app web:dev",
    "web:build": "pnpm -C app web:build",
    "lab": "pnpm -C app lab",
    "lab:web": "pnpm -C app lab:web",
    "package": "pnpm -C app package",
    "make": "pnpm -C app make",
    "lefthook": "pnpm -C app exec lefthook"
  }
}
